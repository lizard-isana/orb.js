# orb.js v3.1 implementation plan — revised

Status: M0 through M7-B completed for the withdrawn 3.1.0 artifact; browser
support, `AstroInstant`, and the 3.1.1 release-audit corrections are complete
locally; Firefox/WebKit execution and the final publication decision remain.

Post-publication correction (2026-09-24):

- `3.1.0` was published for only a few minutes, has no known users, and is
  deprecated on npm.
- The structured astronomical time class is renamed from the easily confused
  `Instant` to `AstroInstant`. The withdrawn spelling is not retained as an
  export alias.
- The browser baseline is Chrome/Edge 92+, Firefox 90+, Safari/iOS Safari
  15.4+, Chrome for Android 92+, and Firefox for Android 90+. No runtime
  polyfills or legacy-browser guarantees are provided.
- UMD remains the compatible `window.Orb` surface. Structured browser imports
  currently require a package-aware bundler; native bare imports and direct
  CDN ESM subpaths are not claimed until stable entries and real-browser tests
  are added.
- A generated classic-script preflight can check boundary runtime built-ins
  before loading UMD and return an actionable report without adding polyfills.
  The package root and `/compatibility` subpath expose the same check after a
  successful load. Syntax support remains a browser-test responsibility.
- This diagnostic contract and its limits are recorded in
  `.ai/decisions/2026-09-24-browser-compatibility-preflight.md`.
- These decisions are recorded in
  `.ai/decisions/2026-09-24-browser-support-and-astro-instant.md`. The next
  release candidate is `3.1.1` and requires a new exact tarball review before
  publication.
- A subsequent independent audit found cross-API and boundary defects in
  Kepler conversion, legacy observation, event search, time arithmetic, SGP4
  input/time handling, and lunar coordinate conversion, plus a missing
  python-sgp4 license notice. The accepted corrections and endpoint/time-scale
  contracts are recorded in
  `.ai/decisions/2026-09-24-release-audit-corrections.md`.
- Follow-up audit cases additionally cover preservation of coordinate-center
  metadata across chained rotations, distinction between threshold tangency
  and a true sign-changing crossing, and consistency of public pass-search
  options with the internal culmination search. The same decision record
  contains these refinements.
- Third-audit cases extend center/origin and distance-unit preservation across
  spherical, rectangular, plane, precession, and legacy observation APIs.
  `Luna.phase()` now selects the most recent computed new moon rather than
  returning a negative age when its initial lunation estimate points forward.
  Cross-API composition tests cover both corrections.
- Fourth-audit cases align heliocentric Earth subtraction with explicit au/km
  inputs, clamp non-singular `Cartesian` anomaly calculations at floating-point
  inverse-trigonometric boundaries, and route direct and function-returned
  rectangular observation targets through one conversion path.
- Fifth-audit cases label the geocentric outputs of `Sun` and `Kepler.radec()`
  before they enter composable coordinate conversions, and merge adjacent
  equal-valued samples that bracket one local maximum before refinement.
- Sixth-audit coverage refines local maxima inside the first and last sample
  intervals without treating monotonic endpoints as local maxima. The
  remaining code-freeze boundary and accepted limitations are recorded in
  `.ai/known-issues.md`.
- The v3 root no longer carries the redundant `old/` v1/v2 copies or the
  already-applied `orbjsalpha5.patch`. Historical v2 remains available through
  the frozen `v2` branch and immutable tags; see
  `.ai/decisions/2026-09-24-v3-root-cleanup.md`.
- The initial-audit build at `7ae86bd` passed the focused and full suites,
  deterministic VSOP check, exact-tarball consumers, and a real Chrome
  UMD/structured-bundle smoke test. The build chain is reduced to Rollup 4 and
  Terser 5, and its `npm audit` reported zero known vulnerabilities.
- The follow-up build passes the same local code, generation, and exact-tarball
  checks on Node 24.14.1. A fresh registry audit and real-browser smoke were not
  rerun after the follow-up changes. Firefox and WebKit execution also remains
  to be completed before claiming cross-engine browser verification.
- Final release-candidate validation after the root cleanup passes GitHub CI on
  Node 18/20/22 and regular/minified browser smoke tests on Firefox 153.0 and
  WebKit 26.5. Exact artifact details and the remaining npm authentication and
  registry-audit gates are recorded in
  `.ai/reviews/2026-09-24-v3.1.1-release-candidate.md`.

Progress (2026-09-23):

- Domain suites, provenance-backed ERFA/Horizons/python-sgp4 fixtures, and
  numerical property tests are in place.
- CommonJS, ESM, browser UMD, and optional full-VSOP npm tarball consumers are
  covered on Node.js.
- `npm run bench` now records representative legacy-v3 throughput and the
  relative cost of full versus short VSOP87A without imposing CI timing gates.
- Checksummed CDS VI/81 inputs now reproduce the optional-full VSOP87A modules
  byte for byte; verification also records the legacy-short residual over
  1500–2500 without changing the v3 default model.
- Raw generation inputs and historical coefficient JSON are excluded from the
  npm tarball. The configured Node 18/20/22 CI matrix remains the M0 completion
  confirmation after push.
- Additive `time`, `frames`, `geodesy`, and `vocab` ESM subpaths now provide
  two-part-TT `AstroInstant`, validated vector/state primitives, IAU 2006/2000B
  frame transforms, TEME/ECEF rotation, observer ENU/horizontal conversion,
  WGS-84 round trips, and controlled structured-API tokens.
- M1 passes the stored ERFA and Meeus references, graph and rotating-velocity
  round trips, equator/Tokyo/pole geodetic cases, invalid-path checks, and exact
  npm-tarball imports without changing the legacy top-level bundle.
- M2 adds a bounded universal-variable Kepler solver, state/element conversion,
  a verified legacy `Orb.Kepler` adapter, optional ERFA EPV00 Earth/Sun, and
  explicit adapters from legacy planets, Moon, Sun, Kepler, and SGP4 objects
  to the structured state contract.
- Vallado, independent RK4, invariant, reversal, near-parabolic, ERFA EPV00,
  Horizons Sun, legacy-compatibility, and exact-tarball tests pass locally.
  The optional EPV00 coefficients remain outside the default bundle.
- M3 adds an explicit modern observer pipeline whose omitted options are
  geometric and airless; light time, annual aberration, refraction, and
  structured metadata are independent opt-ins.
- M4 adds bounded `AstroInstant` searches, geometric-by-default rise/set/transit,
  model-consistent lunar elongation/phases/age, and geometric satellite pass
  windows through the optional `events` subpath.
- USNO July 2026 primary-phase times and a frozen v3 ISS/Tokyo pass fixture
  cover the independent and compatibility gates. The legacy/default bundle
  remains unchanged by importing events only through its subpath.
- M5 extracts pure TLE/OMM normalization, fixed-field and object-identity
  validation, shared signed implied-decimal parsing, and opt-in checksums. The
  optional `sgp4` subpath now returns native TEME/Earth/km/km-s states from
  `AstroInstant` while preserving the legacy `Orb.SGP4` and `Orb.Satellite` facade.
- Structured SGP4 retains sub-millisecond TLE epoch precision and matches the
  stored python-sgp4 near-Earth, GEO, Molniya, and GPS-like vectors. Its
  WGS-72 propagation feeds the common TEME-to-ECEF and WGS-84 geodetic path,
  M3 observer, and M4 pass searches directly.
- The M7 documentation, v2.4.1 migration guides, and exact-tarball
  documentation tests are complete locally. The project owner approved the
  public contract on 2026-09-23, so M7-B versioning and release-candidate
  verification may proceed.

Baseline: `v3` at `0a51b6c`

Reference branch: `archive/v4-prototype` at `6646e23` (formerly
`v4-planning`)

Replacement target release: `@lizard-isana/orb@3.1.1`

## 1. Goal

v3.1 is a bridge between the established v3 API and the mature calculation
work explored on the branch now preserved as `archive/v4-prototype`.

It has two deliberately separate surfaces:

1. The existing top-level v3 API remains synchronous and compatible while
   receiving independently verified correctness fixes.
2. New calculation facilities are added through explicit, tree-shakeable
   subpaths instead of replacing the v3 classes.

The release is broader than an SGP4 refactor. It establishes reusable time,
frame, geodetic, state-vector, solver, observer, and event foundations. SGP4 is
one important consumer of those foundations rather than the organizing center
of the release.

Educational publishing is not part of v3.1. The decision and boundary are
recorded in `../decisions/2026-09-22-separate-educational-materials.md`.

## 2. Compatibility and package policy

### Existing v3 surface

The default import preserves the documented v3 API:

- constructors and aliases such as `Orb.Time`, `Orb.Kepler`, `Orb.SGP4`,
  `Orb.Satellite`, planet classes, and `Orb.Observation`
- synchronous methods such as `xyz(date)`, `radec(date)`, `latlng(date)`, and
  `azel(date)`
- current result field names, units, and keyword strings
- CommonJS, ESM, and browser UMD loading
- optional full VSOP87A registration and body subpaths

Correctness does not require preserving demonstrably wrong numbers. Every
intentional numerical change needs an independent reference or invariant, a
before/after record, and explicit frame, center, time-scale, unit, and
tolerance metadata.

### New subpath surface

The proposed additive modules are:

```text
@lizard-isana/orb/time
@lizard-isana/orb/frames
@lizard-isana/orb/geodesy
@lizard-isana/orb/kepler
@lizard-isana/orb/observer
@lizard-isana/orb/events
@lizard-isana/orb/sgp4
@lizard-isana/orb/vocab
@lizard-isana/orb/models/earth-epv00
```

New modules use structured inputs, radians and km internally, and ESM as their
primary format. They must not be imported by the legacy UMD entry unless its
facade actually needs them, so unused optional models and events do not enlarge
the default browser bundle.

Node 18 support must be tested explicitly. If source modules are exported
directly, their ESM interpretation must not depend on Node 22 syntax detection;
use a nested module boundary or generated `.mjs`/ESM output.

The browser target is explicit rather than the open-ended Browserslist
`defaults` query: Chrome/Edge 92+, Firefox 90+, Safari/iOS Safari 15.4+,
Chrome for Android 92+, and Firefox for Android 90+. orb.js ships no runtime
polyfills. IE 11, EdgeHTML, Opera Mini, KaiOS 2.5, and older browsers are out of
scope. Embedded web views are not guaranteed independently of their engine.

The compatible UMD build is the direct classic-script browser entry. The
structured subpaths are supported through package-aware browser bundlers.
Native browser bare specifiers and direct CDN ESM subpaths remain unsupported
until stable URL entries and real Chromium/Firefox/WebKit tests are present.

### v2 line

v3.1 does not include v2 runtime shims. v2 compatibility is maintained through
branches, tags, releases, and versioned documentation.

## 3. Current baseline to preserve

The v3 baseline already includes:

- Vallado-derived SGP4/SDP4 propagation with near-Earth and deep-space cases
- WGS-72 propagation constants, TEME output, and GMST 1982 rotation
- WGS-84 geodetic output
- python-sgp4 2.27 reference vectors
- Alpha-5 catalogue-number decoding
- leap-second, UTC/TT, GAST, and GMST 1982 distinctions
- TT evaluation of ephemeris series
- explicit J2000-to-of-date conversion and a Sun/VSOP cross-check
- elliptic, parabolic, and hyperbolic Kepler behavior
- optional per-body full VSOP87A registration
- committed `dist/` artifacts checked by CI

These behaviors are frozen as fixtures before structural work begins.

## 4. Scope taken from `archive/v4-prototype`

| v4 work | v3.1 treatment |
| --- | --- |
| Horizons, ERFA, Meeus, Vallado, and python-sgp4 references | Adopt with complete fixture metadata |
| Coordinate round trips and orbital invariants | Adopt as property-style tests |
| Benchmark harness | Adopt without fragile CI timing thresholds |
| VSOP87A compiler and self-validation | Adapt to v3 data formats and optional-full policy |
| `AstroInstant`, vector math, frames, WGS-84 helpers | Add through stable subpaths |
| Universal-variable Kepler solver | Add as a pure API, then connect behind the legacy facade |
| EPV00 Earth and Earth-derived Sun | Add as explicit optional models |
| IAU 2006/2000B, light time, aberration, and refraction | Add through an explicit modern observer pipeline |
| Shared vocabulary and structured meta | Add for new structured APIs; do not rewrite legacy keyword strings |
| Rise/set, lunar phases, and satellite passes | Add after time/frame/observer foundations pass |
| Split TLE/OMM parsing and satellite facade | Adapt without losing Alpha-5 or current OMM support |
| Star and constellation catalogue | Optional later subpath, not a v3.1 release gate |
| Educational markers, excerpts, guides, annotated source | Do not port; maintained outside orb.js |
| v4 compatibility shim replacing native v3 | Do not port |

No v4 file is copied wholesale without checking it against fixes made later on
the v3 branch.

## 5. Milestones

### M0 — Evidence, generation, and performance foundation

Purpose: establish the evidence needed to change numerical code safely without
changing public APIs.

Split the current smoke coverage into domain suites while retaining one
`npm test` command:

- time and reference frames
- Sun, Moon, planets, and VSOP
- Kepler and Cartesian/element conversions
- TLE/OMM and SGP4/SDP4
- observer and horizontal coordinates
- package exports and tarball consumers

Keep all current 27 smoke cases green during reorganization.

Every external fixture records its original source and version, generation or
query procedure, instant and time scale, frame and center, units, expected
accuracy class, and test tolerance.

Adopt or adapt these references:

- JPL Horizons topocentric apparent RA/Dec, azimuth/elevation, and range for
  Moon, Sun, and Mars at several instants
- ERFA/SOFA-derived nutation, obliquity, precession, and matrix values
- Meeus examples for sidereal time and lunar position
- Vallado and python-sgp4 near-Earth and deep-space vectors
- independent RK4 comparisons and two-body invariants for Kepler propagation

Horizons fixtures may initially record the legacy residual rather than impose
the modern observer tolerance. The strict tolerance becomes a gate in M3.

Property-style coverage includes:

- vector and matrix inverse/orthogonality
- frame-transform round trips and path consistency
- geodetic/ECEF round trips
- two-body energy and angular-momentum conservation
- forward/backward propagation
- element/state round trips with explicit singular-case conventions

Engineering tools:

- Add a benchmark comparing legacy and new paths where equivalent. Use it to
  detect order-of-magnitude regressions, not as a strict CI timer.
- Adapt the VSOP87A compiler to vendored primary data, deterministic generated
  modules, precision-preserving output, and compile-time truncation checks.
- Keep raw generation inputs out of the published npm tarball.

Completion gate:

- Node 18, 20, and 22 pass the reorganized suite.
- Reference provenance is stored with fixtures.
- Benchmark and VSOP verification tools run reproducibly.
- M0 introduces no runtime numerical change.

### M1 — Additive time, vector, frame, vocabulary, and geodetic core

Purpose: introduce the common structured substrate without replacing v3
objects.

#### Time

Add `AstroInstant` through `@lizard-isana/orb/time` with construction from `Date`,
ISO, Unix milliseconds, and one- or two-part JD; two-part TT storage; explicit
`jd('utc'|'ut1'|'tt')`; and immutable time arithmetic.

Legacy `Orb.Time` remains public and delegates only after equivalence tests.

#### Frames

Add vector/matrix primitives and structured state vectors through
`@lizard-isana/orb/frames`:

```js
{
  t: AstroInstant,
  frame: 'ecliptic-j2000',
  center: 'sun',
  r: Float64Array,
  v: Float64Array | null
}
```

Internal units are km, km/s, and radians. Unknown frames and unsupported paths
throw instead of being guessed from strings.

Initial graph edges cover ecliptic/equatorial J2000, mean/true equatorial of
date, TEME, ECEF, and observer ENU/horizontal frames.

#### Geodesy

Add pure WGS-84 geodetic/ECEF/ENU functions through
`@lizard-isana/orb/geodesy`. After reference tests pass, the legacy observer
may reuse them.

#### Vocabulary

Add controlled quantity, unit, frame, center, correction, effect, and source
names through `@lizard-isana/orb/vocab`. Legacy `coordinate_keywords` and
`unit_keywords` remain accepted and unchanged.

Completion gate:

- ERFA/Meeus frame references pass at declared tolerances.
- Round-trip and unknown-path tests pass.
- New subpaths load on Node 18/20/22.
- Legacy top-level exports remain unchanged.

### M2 — Solvers, reproducible ephemerides, and optional Earth/Sun models

Purpose: add modern numerical models on top of M1 without changing legacy
defaults.

#### Universal-variable Kepler

Expose through `@lizard-isana/orb/kepler`:

- `propagateKepler(r0, v0, dt, mu)`
- `elementsToState(elements, mu)`
- `stateToElements(state, mu)`

Require one bounded propagation path for all conics, continuity through
`e = 1`, finite and physical input validation, explicit non-convergence
failure, defined singular-case conventions, and no global `Math` mutation.

After independent and legacy comparisons pass, connect existing `Orb.Kepler`
methods through an AU/AU-per-day adapter. Preserve legacy method names and
result shapes.

#### VSOP87A generation

- Generate or verify coefficients from vendored official VSOP87A data.
- Preserve current short-table defaults and optional full registration until a
  separately verified model switch is approved.
- Record input checksums, term counts, validity interval, and worst truncation
  error.

#### Optional EPV00 Earth and derived Sun

Expose EPV00 through a model subpath or new structured body, not as a silent
legacy default. The corresponding Sun is the negated Earth state so both share
one theory.

#### Body/state adapter

Define the minimal body contract:

```js
body.state(instant) -> structured state
```

Provide adapters for existing v3 planets, Moon, Sun, Kepler, and SGP4 objects
so later modules can consume legacy instances without duplicating theories.

Completion gate:

- Vallado, RK4, invariant, time-reversal, and near-parabolic tests pass.
- Legacy Kepler differences are within tolerance or documented as fixes.
- VSOP generation is deterministic and self-validating.
- EPV00 and derived Sun pass ERFA/Horizons references.
- Optional models do not enter the default bundle.

### M3 — Explicit modern observer and accuracy options

Purpose: combine M1/M2 into a coherent optional apparent-place pipeline rather
than scattering unrelated flags through legacy functions.

Add `@lizard-isana/orb/observer` with:

- IAU 2006 precession and IAU 2000B nutation
- separately selectable light-time iteration and annual aberration
- diurnal parallax through common-frame vector subtraction
- optional refraction using supplied pressure and temperature
- optional structured meta

Example direction:

```js
site.observe(body, instant, {
  frameModel: 'iau2006-2000b',
  lightTime: true,
  aberration: true,
  refraction: { pressure: 1010, temperature: 10 },
  meta: true
});
```

The third argument is optional. Omitting it is exactly equivalent to the
following explicit geometric, airless configuration:

```js
site.observe(body, instant, {
  frameModel: 'iau2006-2000b',
  lightTime: false,
  aberration: false,
  refraction: false,
  meta: false
});
```

This default always uses the modern frame model and topocentric vector
subtraction, but it does not choose apparent-place or atmospheric corrections
on the caller's behalf. Light time, annual aberration, refraction, and
structured metadata are independently and explicitly enabled. The structured
observer boundary uses radians and km; refraction conditions use hPa and
degrees Celsius.

Compatibility rules:

- legacy `Observation.azel(date)` keeps its default behavior
- legacy opt-in uses a new options argument or named method
- refraction is not applied unless requested
- structured meta is off by default for legacy calls

Meta identifies quantity, unit, frame, center, corrections, source, accuracy,
and ignored effects. It does not generate prose or recommendations.

Completion gate:

- Horizons Moon/Sun/Mars topocentric fixtures pass their model tolerances.
- Geometric and apparent options have measured, documented differences.
- Range, geocentric distance, RA/Dec, and azimuth/elevation stay distinct.
- Invalid model combinations fail explicitly.

### M4 — Events as additive modules

Purpose: add useful high-level calculations that exercise the complete new
pipeline without changing legacy classes.

Add `@lizard-isana/orb/events` with shared bounded search primitives and:

- rise, set, and transit
- lunar elongation, principal phases, and age
- satellite passes with configurable minimum elevation

Requirements:

- searches operate on `AstroInstant`
- event definitions state whether elevation is geometric or refracted
- horizon constants and semidiameter conventions are documented
- step and refinement tolerances are configurable and bounded
- geometric satellite passes do not imply optical visibility or sunlight

The M4 default contract follows M3: omitted options use geometric positions
without atmospheric refraction. Rise/set uses a zero-radian geometric horizon
and the body center (`semidiameter: 0`); satellite passes use a zero-radian
minimum elevation. Callers opt into refraction through `observation.refraction`
and opt into upper-limb or conventional horizons through explicit values.
`HORIZON_CONSTANTS` publishes mean solar/lunar semidiameters, standard horizon
refraction, and combined conventional center elevations, but none is selected
automatically. A combined constant must not be used together with the same
explicit refraction or semidiameter correction.

Public results and options use radians, km, and `AstroInstant`. Rise/set and pass
results label elevation as `geometric` or `refracted`; pass results also label
optical visibility and sunlight as `not-computed`. Shared searches cap steps at
one day, tolerance at one hour, iterations at 128, and evaluations at one
million. Rise/set further caps sampling steps at six hours and satellite pass
searches at five minutes so a caller cannot silently select an obviously
inappropriate coarse grid.

Completion gate:

- event ordering and threshold re-evaluation properties pass.
- phase times agree with an independent reference.
- pass prediction passes fixed TLE/site fixtures.
- importing events does not enlarge the default entry.

Local completion evidence (2026-09-23): all four gates pass in
`test/m4/core.mjs` and the exact-tarball package suite. The configured Node
18/20/22 CI matrix remains pending until push.

### M5 — SGP4/TLE/OMM integration and hardening

Purpose: connect the mature propagation core to M1 and complete parser
hardening.

Work:

- Extract pure TLE and OMM parsing/normalization from `src/orb-sgp4.js`.
- Preserve `Orb.SGP4`, `Orb.Satellite`, `omm`, `tle`, `orbital_elements`,
  `xyz()`, and `latlng()`.
- Add a structured satellite state tagged TEME/earth/km/km-s.
- Use M1 `AstroInstant`, TEME→ECEF, and WGS-84 in the additive path.
- Preserve numeric and Alpha-5 catalogue numbers.
- Verify both TLE lines identify the same object.
- Parse signed implied-decimal fields in one helper.
- Preserve OMM and stored user-defined TLE lines.
- Keep checksum enforcement opt-in or separately callable.
- Keep WGS-72 for propagation and WGS-84 for geodesy.
- Label TLE data as SGP4 mean elements and B* as the SGP4 drag term, not a
  physical ballistic coefficient.

Tests retain near-Earth, GEO, Molniya, and GPS-like python-sgp4 vectors and add
Alpha-5 propagation, TLE/OMM equivalence, malformed input, object mismatch,
epoch precision, propagation errors, frame conversion, observer integration,
and pass prediction.

Completion gate:

- Valid legacy inputs preserve current vectors at existing tolerance.
- Parser failures have stable explanatory errors.
- Legacy SGP4 contracts show no unintended API change.
- Structured satellites work with M3 observer and M4 passes.

Local completion evidence (2026-09-23): the structured near-Earth and
deep-space vectors meet the python-sgp4 fixture tolerance; Alpha-5, TLE/OMM
equivalence, malformed fields, object mismatch, opt-in checksums, epoch
precision, propagation errors, frame/geodetic conversion, observer use, pass
prediction, exact-tarball import, and legacy facade tests pass. The configured
Node 18/20/22 CI matrix remains pending until push.

### M6 — Legacy adapters and package verification

Purpose: complete and verify the dual-surface implementation without declaring
it release-ready or changing the package version.

Legacy integration:

- Correct legacy WGS-84 observer conversion through the tested M1 helper.
- Remove the time/obliquity circular dependency.
- Turn unsupported coordinate/unit inputs into explanatory errors where this
  does not reject valid historical inputs.
- Keep numerical and format-only changes in separate checkpoints.

Package verification:

- Use `@lizard-isana/orb` with public access.
- Keep project name `orb.js` and browser global `Orb`.
- Exclude raw build inputs and unexported generation data from the tarball.
- Test the exact tarball in temporary consumers: legacy CJS/ESM/UMD, optional
  full VSOP, and every exported new subpath.
- Record file count, packed and unpacked size, and default-bundle size.

Completion gate:

- Legacy observer changes have independent references and before/after records.
- Valid historical coordinate and unit inputs remain accepted while unsupported
  inputs fail with stable explanatory errors.
- The time/obliquity cycle is removed without an unintended numerical change.
- Full tests, build, committed-`dist/` synchronization, generation checks, and
  `git diff --check` pass locally.
- The exact tarball passes legacy and new-subpath consumer tests and contains no
  unexported generation inputs.
- `package.json` remains at the pre-release version until M7 documentation
  review is complete.

Local completion evidence (2026-09-23):

- Legacy observer positions now use the tested M1 WGS-84 geodetic-to-ECEF
  helper. The frozen before/after fixture covers the equator, Tokyo, a 1000 km
  site, and the pole; the correction is about 0.084 m at the Tokyo 25 m site
  and 3.75 km in the deliberately high-altitude case.
- Unsupported or incomplete legacy rectangular coordinate and unit inputs now
  produce stable explanatory errors. Valid plain RA/Dec, tagged
  rectangular, `radec()`, `xyz()`, and numeric-string inputs remain covered.
- The time/obliquity module cycle is removed. Frozen results from 1400 through
  2026, including both sides of the legacy Julian/Gregorian transition, retain
  exact JD, TT-offset, obliquity, nutation, GMST82, and GAST values; Rollup no
  longer emits the circular-dependency warning.
- The exact package tarball passes CommonJS, ESM, browser UMD, direct `dist`,
  legacy `src`, all nine structured subpaths, package metadata, and all eight
  optional full-VSOP body imports. Raw tests, `.ai`, tooling, VSOP generation
  inputs, old sources, GitHub configuration, and `.DS_Store` are excluded.
- The verified pre-M7 artifact contains 78 files, is 3,345,717 bytes packed and
  12,649,450 bytes unpacked, and has a 696,690-byte minified default bundle
  (268,989 bytes through gzip). The package remains version `3.0.0`.
- Full tests, build with committed-`dist/` synchronization, deterministic VSOP
  checks, syntax checks, and `git diff --check` pass locally on Node 24.14.1.
  Node 18/20/22 clean-checkout verification remains an M7 release gate.

### M7 — Documentation review and release

Purpose: review and approve the public v3.1 contract, then prepare and publish
the exact artifact verified for release.

Documentation:

- Update English and Japanese usage together.
- Add separate English and Japanese v2.4.1→v3.1 migration guides for the main
  upgrade audience. Use a staged path: distribution first, compatible numeric
  review second, optional structured API adoption third.
- Add v3.0→v3.1 migration notes and scoped npm imports.
- Document legacy versus structured APIs, models, frames, time scales, units,
  accuracy, and ignored effects.
- Document SGP4 TEME/WGS-72/WGS-84, mean-element, B*, and prediction limits.
- Keep educational chapters, exercises, annotated source, and teaching demos
  outside this repository.
- Include the migration guides in the npm tarball and link them from the
  README and usage guides.

Documentation review gate:

- Every documented import and example runs against the exact packed artifact.
- English and Japanese descriptions agree on defaults, units, frames, models,
  accuracy, and ignored effects.
- Migration notes distinguish compatibility-preserving additions from
  intentional numerical corrections.
- The public documentation is reviewed and approved before versioning begins.
  Approved by the project owner on 2026-09-23.

Local documentation draft evidence (2026-09-23):

- `readme.md` now presents the compatible and structured API surfaces and
  links to complete English/Japanese usage and migration documents.
- English and Japanese usage guides cover the same subpaths, defaults, units,
  frames, models, SGP4 semantics, accuracy scope, and ignored effects.
- Separate English and Japanese v2.4.1→v3.1 guides use the staged migration
  path and distinguish preserved call shapes from intentional numerical and
  semantic corrections. The shorter v3.0→v3.1 notes remain in the usage
  guides.
- `test/m7/docs.mjs` verifies local links and bilingual contract tokens, then
  installs the exact tarball and runs the documented legacy, structured
  observer/event, frame/geodesy/Kepler, structured SGP4, full-VSOP, and browser
  UMD examples.
- The documentation draft passes the full test suite, build with committed
  `dist/` synchronization, deterministic VSOP validation, and
  `git diff --check` locally on Node 24.14.1.
- The documentation tarball contains 80 files, is 3,358,094 bytes packed and
  12,685,470 bytes unpacked. That draft remained version `3.0.0`; public
  review was subsequently approved on 2026-09-23 before the M7-B version
  change.

Release preparation and gate:

- Tests pass on Node 18/20/22 from a clean checkout.
- Build, committed-`dist/` synchronization, and `git diff --check` pass.
- Generation tools reproduce committed generated files.
- Tarball consumer and CDN tests pass.
- The withdrawn `3.1.0` remains deprecated; prepare `3.1.1` only after the
  replacement documentation and compatibility changes are otherwise ready.
- Publish the exact verified `3.1.1` artifact under npm `next`, then move that
  same version to `latest` only after installation and CDN verification.

Local release-candidate evidence (2026-09-23):

- `package.json` and the root lockfile entries are version `3.1.0` at
  release-preparation commit `628ddff`.
- Clean-checkout CI-equivalent runs pass on Node 18.20.8, 20.12.2, and 22.23.2:
  `npm ci`, build, committed-`dist/` comparison, and the full test suite.
- Deterministic VSOP verification, `git diff --check`, exact-tarball consumers,
  and `npm audit --omit=dev` pass. The published package has no runtime
  dependencies or reported runtime vulnerabilities.
- The exact 80-file candidate is 3,358,093 bytes packed and 12,685,470 bytes
  unpacked. Its SHA-256 is
  `597d35e68faf214668c3914d52ce6cd3f6ef17e52ea352222f5f141e68256a95`;
  a clean clone reproduces it byte for byte.
- `npm publish --dry-run --tag next --access public` succeeds for that file.
  The registry lookup returned `E404` and this host is not authenticated
  (`npm whoami` returned `E401`), so no external publication occurred.
- Registry installation, CDN verification, release tagging, and later
  `latest` promotion remain post-publication gates. Full evidence and the
  exact command order are recorded in
  `.ai/reviews/2026-09-23-v3.1-release-candidate.md`.

## 6. Proposed checkpoint sequence

1. `test: establish v3.1 references and domain suites`
2. `tooling: add benchmark and reproducible VSOP validation`
3. `feat: add AstroInstant and vector primitives as subpaths`
4. `feat: add explicit frame and WGS-84 transforms`
5. `feat: add shared vocabulary for structured APIs`
6. `feat: add universal-variable Kepler functions`
7. `refactor: connect legacy Kepler through verified adapters`
8. `feat: add optional EPV00 Earth and derived Sun`
9. `feat: add explicit apparent observer pipeline`
10. `feat: add structured observation metadata as an option`
11. `feat: add rise-set, phase, and pass events subpath`
12. `refactor: isolate TLE and OMM parsing`
13. `refactor: connect SGP4 to structured time and frames`
14. `fix: apply verified legacy observer and input corrections`
15. `chore: verify package subpaths and reduce tarball inputs`
16. `docs: document v3.1 APIs, models, and migration`
17. `release: prepare @lizard-isana/orb 3.1.0` (completed, then withdrawn)
18. `fix: clarify browser support and rename AstroInstant for 3.1.1`

Checkpoints 14–15 constitute M6. Checkpoints 16–17 constitute M7, with an
explicit documentation approval gate between them. Version changes, npm
publication, dist-tag promotion, and branch transitions do not occur in M6.

Every numerical checkpoint runs its domain tests, the full suite, build,
`git diff --check`, and committed-`dist/` verification. Generated artifacts
are reviewed separately from handwritten numerical changes.

## 7. Branch and release transition

Use the branch roles recorded in
`.ai/decisions/2026-09-24-v2-v3-branch-roles.md`:

1. Before moving `master`, preserve its current `v2.4.1` tip under an explicit
   `v2` branch and the existing immutable tags/releases.
2. Treat `v2` as a runtime-frozen legacy line. Documentation-only navigation
   corrections are allowed; any code release requires a separate decision.
3. Keep `v3` as the active v3 integration and maintenance branch, and publish
   the exact verified `3.1.1` artifact under npm `next` from that line.
4. After npm installation, CDN, and browser verification, advance `master` to
   the verified v3.1.1 tree without merging old v2 file contents into it.
5. Keep `master` as the GitHub default and stable published branch. They match
   for the 3.1.1 transition; later parallel maintenance follows
   `.ai/decisions/2026-09-25-v3-parallel-maintenance.md`, under which `v3` may
   differ while the next minor is in development.
6. Keep `archive/v4-prototype` reference-only and do not publish it as
   `latest`.
7. Promote `@lizard-isana/orb@3.1.1` from `next` to `latest` only after branch,
   documentation, install, and CDN checks pass.
8. Clean up merged or obsolete topic branches only after `master`, `v3`, and
   `v2` are verified on the remote.

Branch changes, default-branch changes, npm publication, and dist-tag promotion
remain separate explicit checkpoints.

## 8. Out of scope

- removing or replacing the public v3 class API
- async position methods or implicit network loading
- optional full data in the default bundle
- changing legacy defaults without references and migration notes
- full JPL-grade ephemerides
- v2 runtime shims inside v3
- TypeScript rewrite or project-wide format conversion
- making the star/constellation catalogue a release requirement
- educational chapters, exercises, staged teaching implementations,
  source-excerpt synchronization, annotated source, or educational CI
- publishing `archive/v4-prototype` directly to npm `latest`

## 9. Definition of done

v3.1 is complete when:

- legacy contract tests show no unintended API removal;
- new subpaths load independently without enlarging the default bundle;
- reference and property tests explain every numerical change;
- generation tools are deterministic and their inputs have provenance;
- optional EPV00, apparent corrections, meta, and events pass their gates;
- SGP4/TLE/OMM and Kepler pass without tuning constants to fixtures;
- source, generated data, and committed `dist/` are synchronized;
- the exact tarball passes legacy and new-subpath consumer tests;
- migration and model-selection docs exist in English and Japanese;
- the clean release commit is tagged and published under `next`;
- npm install and CDN loading are verified;
- `latest` promotion and default-branch switching require separate approval;
- no educational build or synchronization responsibility remains in orb.js.

## 10. Implementation progress

M0 started on 2026-09-22 with a runtime-neutral test reorganization:

- the original 27 smoke cases are split into time/frame, ephemeris, Kepler,
  SGP4, and observer suites;
- `npm test` remains the single full-suite command;
- `test/smoke.js` remains as a compatibility entry point;
- the required provenance fields for numerical fixtures are documented under
  `test/fixtures/`;
- the existing Meeus, Horizons Sun, JPL GM, and python-sgp4 values have been
  moved into versioned JSON fixtures and are rejected if required provenance
  metadata is absent;
- the Horizons Sun fixture was regenerated with Horizons API 1.2 and DE441
  using recorded query parameters;
- ERFA 2.0.1.5 nutation, obliquity, and selected precession-matrix references
  are stored independently of the runtime implementation;
- Horizons API 1.2 topocentric apparent RA/Dec, azimuth/elevation, and range
  are stored for Moon, Sun, and Mars at a Tokyo site over five instants;
- the unchanged v3 observer baseline is recorded separately from the Horizons
  values. Its measured maximum sky/range residuals are Moon 4.43 arcsec and
  34.89 km, Sun 25.57 arcsec and 4185.61 km, and Mars 28.02 arcsec and
  70237.65 km;
- the unchanged v3 obliquity/nutation baseline differs from ERFA by at most
  0.053 arcsec in mean obliquity, 2.070 arcsec in nutation longitude, and
  0.053 arcsec in nutation obliquity across the fixture dates;
- deterministic property tests now cover RA/Dec-vector round trips,
  ecliptic/equatorial round trips, rotation norm preservation, precession
  round trips, Kepler energy and angular-momentum conservation, and
  periapsis-centered time symmetry for elliptic, parabolic, and hyperbolic
  cases;
- the exact npm tarball is installed into an isolated temporary consumer and
  tested through CommonJS, Node ESM, browser-style UMD evaluation, and a full
  VSOP87A subpath;
- Node ESM now resolves a generated `.mjs` entry and optional VSOP87A modules
  use a nested ESM package boundary, avoiding reliance on post-Node-18 syntax
  detection while retaining the existing `.js` artifacts.

M0 then completed with a non-gating benchmark and checksummed, deterministic
VSOP87A regeneration and legacy-short residual verification.

M1 completed locally on 2026-09-22:

- `@lizard-isana/orb/time` adds immutable `AstroInstant` construction from `Date`,
  ISO, Unix milliseconds, and one- or two-part Julian dates; TT is retained in
  two parts and UTC, UT1 (with explicit optional DUT1), and TT are selectable.
- `@lizard-isana/orb/frames` adds validated `Float64Array` vector/matrix
  primitives and structured states in km and km/s. Its explicit graph covers
  ecliptic/equatorial J2000, mean/true equatorial of date, ecliptic of date,
  TEME, ECEF, observer ENU, and terminal horizontal coordinates.
- The modern frame path uses IAU 2006 precession, IAU 2000B nutation, GMST82
  for TEME/ECEF, and a separately tested GAST path for true-of-date/ECEF.
- `@lizard-isana/orb/geodesy` adds pure WGS-84 geodetic/ECEF and observer
  ENU/horizontal functions, including explicit geocenter rejection and stable
  inverse conversion at both poles.
- `@lizard-isana/orb/vocab` centralizes quantity, unit, frame, center,
  correction, effect, and source tokens while leaving legacy keyword strings
  untouched.
- The exact tarball imports all four new ESM subpaths. The CommonJS/ESM/UMD
  top-level entry remains unchanged and the rebuilt committed `dist/` files
  have no diff.
- Local full-suite, build, syntax, and whitespace checks pass. The configured
  Node 18/20/22 CI matrix remains the final M1 completion confirmation after
  push.

M2 completed locally on 2026-09-22:

- `@lizard-isana/orb/kepler` provides `propagateKepler`, `elementsToState`,
  and `stateToElements` in km, km/s, seconds, radians, and km3/s2. A single
  universal-variable path covers elliptic, parabolic, hyperbolic, forward,
  and backward propagation with bounded safeguarded iteration and explicit
  validation/non-convergence errors.
- The solver matches Vallado example 2-4 and independent RK4 integration for
  all conic classes. Energy, angular momentum, period closure, time reversal,
  Stumpff continuity, and continuity across eccentricity one are covered.
- Legacy `Orb.Kepler` now delegates its three branch methods through an
  AU/AU-per-day adapter without changing public names or result shapes. The
  maximum sampled residual from the frozen v3 baseline is about 1.9e-12 au
  in position and 3.3e-14 au/day in velocity. The old global `Math` fallback
  mutation is removed.
- `@lizard-isana/orb/models/earth-epv00` provides optional heliocentric Earth
  and derived geocentric Sun states from the ERFA/SOFA EPV00 coefficients.
  Earth position/velocity matches ERFA and the derived Sun agrees with the
  independent Horizons fixture at the declared pre-M3 tolerance.
- Explicit legacy-body adapters convert v3 planet, Moon, Sun, Kepler, and
  SGP4 outputs to the M1 structured state contract without guessing from
  legacy keyword strings.
- Existing deterministic full-VSOP verification remains green. Exact-tarball
  consumers load the Kepler and EPV00 subpaths, while a package test confirms
  that EPV00 does not enter the default UMD bundle.
- The benchmark now reports structured versus legacy Kepler throughput. Local
  full-suite, build, VSOP, benchmark, syntax, and whitespace checks pass; the
  Node 18/20/22 CI matrix remains the final M2 confirmation after push.

M3 completed locally on 2026-09-23:

- `@lizard-isana/orb/observer` adds a radians-and-km `createObserver()` /
  `site.observe(body, instant, options)` boundary over the M1 frame and WGS-84
  helpers and M2 body-state adapters.
- Omitting the third argument is explicitly equivalent to IAU 2006/2000B,
  geometric, airless observation with light time, annual aberration,
  refraction, and metadata all disabled. Each correction is independently
  selectable; refraction additionally requires pressure in hPa and
  temperature in degrees Celsius.
- Heliocentric targets are translated with the optional EPV00 Earth state,
  light time uses bounded retarded-time iteration, annual aberration preserves
  range while changing direction, and observer parallax/range use common-frame
  ECEF vector subtraction.
- Results keep right ascension, declination, azimuth, elevation, topocentric
  range, and geocentric distance distinct. Optional metadata identifies each
  quantity, unit, frame, center, applied corrections, sources, accuracy when
  declared by the body, and ignored effects.
- Retarded states from both heliocentric and geocentric body models are reduced
  through one heliocentric geometry, so light time and annual aberration remain
  independent without double-counting Earth's orbital motion. Both corrections
  are enabled for the apparent Moon, Sun, and Mars comparisons. Across the
  stored Tokyo fixture, maximum equatorial /
  horizontal / range residuals are respectively about 7.16 arcsec / 7.23
  arcsec / 2.50 km for the Moon, 0.26 arcsec / 0.22 arcsec / 2.71 km for the
  Sun, and 0.36 arcsec / 0.30 arcsec / 37.77 km for Mars.
- The exact npm tarball imports the observer subpath, while the legacy
  CommonJS/ESM/UMD top-level API and default bundle remain unchanged. Local
  benchmark output compares structured and legacy Moon observation without a
  timing gate. Local full-suite, build, VSOP, benchmark, syntax, and whitespace
  checks pass; the Node 18/20/22 CI matrix remains the final M3 confirmation
  after push.
