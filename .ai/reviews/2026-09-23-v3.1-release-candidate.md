# @lizard-isana/orb 3.1.0 release candidate

Date: 2026-09-23

Status: release preparation complete locally. npm authentication, publication
under the `next` dist-tag, registry installation, CDN verification, and later
promotion to `latest` remain external release gates.

## Source

- Release-preparation commit: `628ddff447af0d5911b2f1fc4c9d1fdf5437f6c9`
- Branch: `v3`
- Package name: `@lizard-isana/orb`
- Package version: `3.1.0`
- Node engine: `>=18`
- Publish configuration: public package on `https://registry.npmjs.org/`

The version is synchronized in `package.json` and both root entries in
`package-lock.json`. Documentation approval preceded the version change.

## Clean-checkout verification

A local clone of the release-preparation commit was clean before and after each
CI-equivalent run. Each runtime used npm 10.9.9 and ran:

1. `npm ci`
2. `npm run build`
3. `git diff --exit-code dist/`
4. `npm test`

Results:

| Runtime | Result |
|---|---|
| Node 18.20.8 | pass |
| Node 20.12.2 | pass |
| Node 22.23.2 | pass |

`npm run vsop:check`, `git diff --check`, and the final clean status also pass.
The full tests include exact-tarball CommonJS, ES module, browser UMD, every
structured subpath, optional full-VSOP modules, and documentation examples.

`npm audit --omit=dev` reports zero vulnerabilities. A full development install
reports 15 findings in the historical build-tool dependency tree (1 low,
2 moderate, 10 high, 2 critical); these dependencies are not present in the
published package and there are no runtime dependencies. Build-tool
modernization is a separate maintenance task and should not be mixed into the
verified astronomy release candidate without a new build review.

## Exact artifact

Path used for local verification:

`/tmp/orb-v3.1.0-rc/lizard-isana-orb-3.1.0.tgz`

| Property | Value |
|---|---|
| Files | 80 |
| Packed bytes | 3,358,093 |
| Unpacked bytes | 12,685,470 |
| npm SHA-1 | `1c78e63412644bc4a0f6fc82e8cc9624647c8477` |
| SHA-256 | `597d35e68faf214668c3914d52ce6cd3f6ef17e52ea352222f5f141e68256a95` |
| npm integrity | `sha512-hFPpUin5KhEnqAixxTwb2pgrNTtImYMGg5L8oSqTkNBDGl4bHZpl2ZYWDg5D5wO78Nq/vrLC7855bSvWXL3P5w==` |
| Minified UMD | 696,690 bytes |

Packing the clean clone reproduced the tarball byte for byte. Installing this
exact file and running consumers succeeded for:

- legacy CommonJS on Node 18;
- structured ES modules and optional full Mars VSOP87A on Node 20;
- browser-style UMD evaluation on Node 22.

`npm publish --dry-run --tag next --access public` succeeds for this exact
file and reports the same name, version, file count, sizes, SHA-1, and integrity.

## Registry state and remaining gates

An unauthenticated registry lookup for `@lizard-isana/orb` returned `E404`.
This is consistent with an initial public publication, but package/scope access
must be confirmed after authenticating the intended npm account. `npm whoami`
currently returns `E401`, so no publication was attempted.

After authentication and explicit publication approval:

1. Reconfirm `npm whoami` and scope access.
2. Reconfirm the candidate SHA-256.
3. Publish the exact tarball with public access and dist-tag `next`.
4. Read registry metadata and confirm `next` resolves to `3.1.0` while
   `latest` remains unchanged.
5. Install `@lizard-isana/orb@next` in a fresh consumer and repeat CJS, ESM,
   subpath, full-VSOP, and UMD checks.
6. Verify the pinned unpkg `dist/orb.js` and minified UMD, including the browser
   global and response integrity/size.
7. Create the final release tag only for the verified source commit.
8. Promote the same `3.1.0` artifact to `latest` only after the agreed
   observation period; do not repack or republish it.
